services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rooted-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: rooted
      RABBITMQ_DEFAULT_PASS: rooted